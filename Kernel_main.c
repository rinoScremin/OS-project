#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>
#include <Kernel_print.h>


void kernel_main(void) 
{
    /*
    unsigned char *vidmem = (unsigned char *)0xB8000;
    vidmem[0] = 'H'; // First character
    vidmem[1] = 0x07; // First attribute byte

    vidmem[4] = 'E'; // Second character (manually calculated offset)
    vidmem[5] = 0x07; // Second attribute byte

    vidmem[8] = 'L'; // Third character (manually calculated offset)
    vidmem[9] = 0x07; // Third attribute byte

    
    */
   while (1);
}
